# Keep this folder in git so the rust-embed macro in src-tauri compiles on a
# fresh checkout. Built web UI assets are gitignored (see app/web/.gitignore);
# an empty dist embeds as an empty asset set → the web routes 404 until built.
